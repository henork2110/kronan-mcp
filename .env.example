# Port to run the server on (Railway sets this automatically)
PORT=3000

# Optional: a shared Krónan token for testing only.
# In production, each user provides their own token via Poke Connections.
# KRONAN_TEST_TOKEN=your_token_here
